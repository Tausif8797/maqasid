// Generated by GitHub Copilot
const express = require('express')
const {
  getSettings,
  updateSettings,
} = require('../controllers/contributionSettingController')
const { protect, authorize } = require('../middleware/authMiddleware')
const validate = require('../middleware/validate')
const { updateSettingsRules } = require('../middleware/contributionSettingValidator')

const router = express.Router()

// Every settings route requires an authenticated admin.
router.use(protect, authorize('admin'))

// GET /api/settings/contributions
router.get('/contributions', getSettings)

// PUT /api/settings/contributions
router.put('/contributions', updateSettingsRules, validate, updateSettings)

module.exports = router