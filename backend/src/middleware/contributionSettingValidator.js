// Generated by GitHub Copilot
const { body } = require('express-validator')

/** Validation rules for updating contribution settings (all fields optional). */
const updateSettingsRules = [
  body('monthlyAmount')
    .optional()
    .isFloat({ min: 1 })
    .withMessage('Monthly amount must be at least 1'),
  body('description')
    .optional()
    .trim(),
  body('isActive')
    .optional()
    .isBoolean()
    .withMessage('isActive must be a boolean'),
]

module.exports = { updateSettingsRules }
