// Generated by GitHub Copilot
const asyncHandler = require('../utils/asyncHandler')
const memberDashboardService = require('../services/memberDashboardService')

/**
 * @desc    Get logged-in member's dashboard data
 * @route   GET /api/member/dashboard
 * @access  Private (member)
 */
const getMyDashboard = asyncHandler(async (req, res) => {
  const dashboard = await memberDashboardService.getMemberDashboard(req.user._id)
  res.status(200).json({
    success: true,
    data: dashboard,
  })
})

/**
 * @desc    Get all active loans across all members
 * @route   GET /api/member/dashboard/active-loans
 * @access  Private (member)
 */
const getAllActiveLoans = asyncHandler(async (req, res) => {
  const loans = await memberDashboardService.getAllActiveLoans()
  res.status(200).json({
    success: true,
    data: loans,
  })
})

module.exports = { getMyDashboard, getAllActiveLoans }
