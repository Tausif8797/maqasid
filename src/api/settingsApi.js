// Generated by GitHub Copilot
import { apiRequest } from './client.js'

// Contribution settings endpoints (Phase 3).
export const settingsApi = {
  /** Get the current contribution settings. */
  get: () =>
    apiRequest('/settings/contributions').then((r) => r.data),

  /** Update the contribution settings. */
  update: (data) =>
    apiRequest('/settings/contributions', {
      method: 'PUT',
      body: data,
    }).then((r) => r.data),
}